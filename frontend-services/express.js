const express = require('express');
const expApp = express();
expApp.use(express.json());

const OK = 200;
const FAIL = 404;

const sifExpressServer = () => {

  //add GET, POST, PUT, DELETE, ETC here
}